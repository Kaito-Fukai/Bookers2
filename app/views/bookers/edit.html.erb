<h2>edit post</h2>

<% if @post.user.id == current_user.id %>

	<%= form_for(@post) do |f| %>
		<h4> title </h4>
		<%= f.text_field :title %>

		<h4> impression </h4>
		<%= f.text_area :impression %>

		<%= f.submit '編集完了' %>
	<% end %>

<% else %>
	<p> 編集権限がありません </p>
<% end %>